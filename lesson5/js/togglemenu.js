function toggleMenu() {
    // document.querySelectorAll(".navigation li") 
    // console.log(document.querySelectorAll(".navigation li"))
    // var nav = document.querySelectorAll(".navigation li");
    // var index;
    // for (index = 0; index < nav.length; index++) {
    //    nav [index] .style.display="block";
    // }
    document.querySelector("nav ul") .classList.toggle("navigation")
}